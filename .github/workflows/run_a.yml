name: A-Share Screener

on:
  schedule:
    - cron: '30 8 * * 1-5'
  workflow_dispatch: 

jobs:
  run-screener:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4   # 👈 升级为 v4

      - name: Set up Python
        uses: actions/setup-python@v5  # 👈 升级为 v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Create credentials.json
        run: |
          echo '${{ secrets.GCP_CREDENTIALS }}' > credentials.json

      - name: Run A-Share Screener
        run: python run_a.py
